---
phase: 02-display-rendering-and-theming
plan: 04
type: execute
wave: 3
depends_on:
  - 02-03
files_modified:
  - scripts/session-start.js
autonomous: false
requirements:
  - DISP-01
  - DISP-02
  - DISP-03
  - DISP-05
  - DISP-06

must_haves:
  truths:
    - "Starting a Claude Code session displays the full framed panel (not Phase 1 plain-text dashes)"
    - "The panel shows mosque art, Arabic text, transliteration, translation, and surah reference"
    - "Narrow terminal (< 60 cols) shows the box frame but no mosque art"
    - "NO_COLOR=1 produces the same content with no color codes"
    - "Session-start always displays — no rate-limit check present in session-start.js"
  artifacts:
    - path: "scripts/session-start.js"
      provides: "SessionStart hook — upgraded from Phase 1 plain-text to full renderPanel output"
  key_links:
    - from: "scripts/session-start.js"
      to: "scripts/lib/render-panel.js"
      via: "require('./lib/render-panel')"
      pattern: "renderPanel"
    - from: "scripts/session-start.js"
      to: "scripts/lib/select-ayah.js"
      via: "require('./lib/select-ayah')"
      pattern: "selectAyah"
---

<objective>
Upgrade session-start.js from the Phase 1 plain-text formatAyah() output to the full renderPanel() framed display. Then verify the complete Phase 2 experience in a live Claude Code session.

Purpose: session-start.js is the first hook users see on every session open. Switching it to the full panel completes the visual upgrade for the primary entry point. The human checkpoint confirms the end-to-end ANSI rendering works correctly in the actual terminal — something unit tests cannot verify.
Output: scripts/session-start.js (modified) + human approval of live panel rendering
</objective>

<execution_context>
@/Users/amu1o5/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amu1o5/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-display-rendering-and-theming/02-CONTEXT.md

@scripts/session-start.js
@hooks/hooks.json

@.planning/phases/02-display-rendering-and-theming/02-01-SUMMARY.md
@.planning/phases/02-display-rendering-and-theming/02-02-SUMMARY.md
@.planning/phases/02-display-rendering-and-theming/02-03-SUMMARY.md
</context>

<interfaces>
<!-- Current session-start.js contract (Phase 1) being replaced: -->

Phase 1 formatAyah() function (DELETE this):
```javascript
function formatAyah(ayah) {
  return ['', '------', ayah.arabic, ayah.transliteration,
          '"' + ayah.translation + '"',
          '\u2014 ' + ayah.surah_name + ' ' + ayah.surah_number + ':' + ayah.ayah_number,
          '------', ''].join('\n');
}
```

Phase 2 replacement (ADD these requires and call):
```javascript
const { selectAyah } = require('./lib/select-ayah');
const { renderPanel } = require('./lib/render-panel');

// In main():
const sessionId = process.env.CLAUDE_SESSION_ID || '';
const ayah = selectAyah('', sessionId, pluginRoot);  // '' = no tool name → time-of-day theme
// ...
const displayText = renderPanel(ayah, { cols: process.stdout.columns || 80 });
```

Critical constraint (from Phase 1 empirical test + RATE-03):
- SessionStart has NO stdin JSON — do NOT call fs.readFileSync(0) (Pitfall 4: hangs forever)
- SessionStart is NEVER rate-limited — do NOT add any cooldown check (RATE-03)
- resolvePluginRoot() stays verbatim — three-strategy fallback still needed
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade session-start.js to use renderPanel</name>
  <files>scripts/session-start.js</files>
  <action>
    Modify scripts/session-start.js (read the current file first — content is at line 1-73).

    Changes required:
    1. Add two new requires after existing requires:
       ```javascript
       const { selectAyah } = require('./lib/select-ayah');
       const { renderPanel } = require('./lib/render-panel');
       ```

    2. DELETE the formatAyah() function entirely (lines 41-52 in current file).

    3. Replace the main() function body:
       - KEEP: `const pluginRoot = resolvePluginRoot();`
       - REPLACE: `const ayah = loadAyah(pluginRoot, 'ilm');` with:
         ```javascript
         const sessionId = process.env.CLAUDE_SESSION_ID || '';
         const ayah = selectAyah('', sessionId, pluginRoot);
         ```
         Note: toolName is '' for SessionStart (no tool in context); selectAyah falls back to time-of-day theme.
       - KEEP: null check `if (!ayah) { ... empty systemMessage ... exit 0 }`
       - REPLACE: `const displayText = formatAyah(ayah);` with:
         ```javascript
         const displayText = renderPanel(ayah, { cols: process.stdout.columns || 80 });
         ```
       - KEEP: `process.stdout.write(JSON.stringify({ systemMessage: displayText })); process.exit(0);`

    4. REMOVE the `loadAyah` require (no longer used directly — selectAyah wraps it):
       - Remove: `const { loadAyah } = require('./lib/load-ayah');`

    5. resolvePluginRoot() function: leave completely unchanged (verbatim from Phase 1).

    6. Outer try/catch: leave unchanged.

    NEVER add rate-limit logic. NEVER add stdin reading. NEVER add `async: true` anywhere.

    After edit, do a quick syntax check: `node --check scripts/session-start.js`
  </action>
  <verify>
    <automated>node --check scripts/session-start.js && grep -c "loadAyah\|formatAyah\|isWithinCooldown\|readFileSync(0" scripts/session-start.js | xargs -I{} test {} -eq 0 && echo "session-start.js clean"</automated>
  </verify>
  <done>
    - node --check passes (no syntax errors)
    - session-start.js contains zero references to: loadAyah, formatAyah, isWithinCooldown, readFileSync(0
    - session-start.js contains: selectAyah, renderPanel, resolvePluginRoot
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full Phase 2 panel rendering in live session</name>
  <files>none</files>
  <action>
    Run the full test suite to confirm automated tests pass, then instruct user to start a live Claude Code session and verify the visual output.

    Run before handing to user:
    `node scripts/lib/render-panel.test.js && node scripts/lib/select-ayah.test.js && node scripts/pre-tool-use.test.js`

    All three must exit 0 before proceeding to human verification.
  </action>
  <verify>
    <automated>node scripts/lib/render-panel.test.js && node scripts/lib/select-ayah.test.js && node scripts/pre-tool-use.test.js</automated>
  </verify>
  <done>User confirms: framed green panel with mosque art visible on session start; PreToolUse rate limiting suppresses second display within 60s; NO_COLOR=1 produces plain-text box</done>
  <what-built>
    Full Phase 2 display pipeline:
    - scripts/lib/render-panel.js — framed Unicode box with mosque art, chalk colors, NO_COLOR support
    - scripts/lib/select-ayah.js — tool-type + time-of-day theme resolution, session no-repeat
    - scripts/pre-tool-use.js — PreToolUse hook with 60-second rate limiting
    - scripts/session-start.js — upgraded from plain-text dashes to full framed panel
    - hooks/hooks.json — PreToolUse entry registered
  </what-built>
  <how-to-verify>
    **Test 1 — Session start panel:**
    1. From the repo root, start a Claude Code session with this plugin:
       `claude --plugin-dir .`
    2. On session open, you should see a framed green Unicode box (not the Phase 1 plain `------` dashes)
    3. Confirm the box contains: dome/minaret ASCII art, Arabic text, transliteration, English translation, surah reference in green

    **Test 2 — PreToolUse rate limiting:**
    4. In the same session, ask Claude to read a file (e.g. "read package.json")
    5. Confirm an ayah panel displays when Claude uses the Read tool
    6. Immediately ask Claude to read another file — confirm NO second panel (rate limited, 60s cooldown)
    7. Wait 60+ seconds, then ask Claude to read another file — confirm panel appears again

    **Test 3 — Theme routing:**
    8. Ask Claude to run a bash command — confirm panel shows (tawakkul-themed ayahs relate to reliance/effort)
    9. Ask Claude to write or edit a file — confirm panel shows (ihsan theme — excellence)

    **Test 4 — NO_COLOR:**
    10. Exit the session. Re-run with: `NO_COLOR=1 claude --plugin-dir .`
    11. Confirm the framed box appears but with no green color (plain box-drawing characters only)

    **Test 5 — Narrow terminal (optional):**
    12. Resize terminal to under 60 columns, start a new session
    13. Confirm no mosque art appears but the box frame is still present
  </how-to-verify>
  <resume-signal>Type "approved" if the panel renders correctly, or describe any visual issues (wrong colors, missing art, broken box characters, etc.)</resume-signal>
</task>

</tasks>

<verification>
Syntax check: `node --check scripts/session-start.js`
Removed legacy: `grep -c "formatAyah\|loadAyah" scripts/session-start.js` must output 0
Full test suite: `node scripts/lib/render-panel.test.js && node scripts/lib/select-ayah.test.js && node scripts/pre-tool-use.test.js`
</verification>

<success_criteria>
- node --check passes for all modified scripts
- Full test suite passes (render-panel, select-ayah, pre-tool-use)
- Human checkpoint: framed panel with mosque art and colors displays on session start
- Human checkpoint: PreToolUse rate limiting confirmed (second call within 60s suppressed)
- Human checkpoint: NO_COLOR=1 produces plain-text box (no ANSI codes)
- All Phase 2 requirements (DISP-01–03, DISP-05, DISP-06, THEME-01–04, RATE-01–03, HOOK-02) satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/02-display-rendering-and-theming/02-04-SUMMARY.md`
</output>
